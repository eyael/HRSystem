@model LabExercise.Models.LabTestDue

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>LabTestDue</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.patientName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select name="ddlPatient" id="ddlPatient">
                    <option value="Eyael">Eyael</option>
                    <option value="Mikre">Mikre</option>

                </select>
          
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.dateTimeStamp, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <form action="/action_page.php">

                    <input type="datetime-local" >
                    <input type="submit">
                </form>
              
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
